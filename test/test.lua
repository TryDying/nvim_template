-- template = require('lua.template.init')
-- --
-- template.setup({
--     template_paths = {
--         ccls = 'test_config',
--     }
-- })
local filename = '.ccls'  -- 或者其他文件名，如'CMakeLists.txt'
local filepath = '~/' .. filename -- 注意：这里假设文件在用户主目录下


